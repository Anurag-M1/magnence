{
  "name": "magnence-web",
  "private": true,
  "version": "0.1.0",
  "description": "Magnence marketing website",
  "engines": {
    "node": ">=20 <27"
  },
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "npm run dev --workspace=@magnence/web",
    "build": "npm run build --workspace=@magnence/web",
    "lint": "npm run lint --workspace=@magnence/web",
    "typecheck": "npm run typecheck --workspace=@magnence/web"
  },
  "devDependencies": {
    "next": "14.2.35"
  }
}
